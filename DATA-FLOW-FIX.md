# 数据流问题修复说明

## 🔧 已修复的问题

### 问题描述
后台更改数据后，前台刷新时看不到更新的内容。

### 根本原因
1. **前台 `main.js`** 在页面加载时会自动从 GitHub 下载数据并**覆盖** localStorage
2. **后台 `admin.js`** 同样会在加载时从 GitHub 同步数据，覆盖用户正在编辑的内容
3. 这导致即使后台保存了数据到 localStorage，前台刷新时也会被 GitHub 的旧数据覆盖

### 修复方案
修改了以下文件的逻辑：

#### 1. `scripts/main.js`
- ✅ 修改 `bootstrapFront()` 函数：只在 localStorage 为空时才从 GitHub 载入
- ✅ 修改 `syncDataFromGitHub()` 函数：同样只在 localStorage 为空时才同步
- ✅ 移除了重复的初始化代码（DOMContentLoaded 监听器）

#### 2. `scripts/admin.js`
- ✅ 修改 `syncDataFromGitHub()` 函数：后台页面不自动覆盖用户正在编辑的数据

## 📋 测试步骤

### 方法1：使用诊断工具
1. 打开 `test-data-flow.html`
2. 点击"添加测试工作"按钮
3. 打开前台页面 `index.html`
4. 检查是否能看到"测试公司 Test Corp"

### 方法2：使用后台管理
1. 打开 `admin.html` 并登录
2. 在"工作经历"区块添加新的工作经验：
   - 公司：测试公司
   - 职位：测试职位
   - 期间：2024-2026
   - 简介：这是测试数据
3. 点击"新增"
4. 打开新标签页，访问 `index.html`
5. 滚动到"学歷與工作經歷"区块
6. ✅ 应该能看到刚才添加的"测试公司"

### 方法3：使用浏览器开发者工具
1. 按 F12 打开开发者工具
2. 进入 Console 标签
3. 输入以下代码查看 localStorage：
   ```javascript
   const data = JSON.parse(localStorage.getItem('cvData'));
   console.log('工作经验:', data.experience.filter(e => e.type === 'Work'));
   ```
4. 应该能看到所有工作经验的列表

## 🐛 如果问题仍然存在

### 清除缓存并重新测试
1. 按 Ctrl + Shift + Delete 打开清除浏览器数据
2. 勾选"缓存的图片和文件"
3. 点击"清除数据"
4. 重新访问 `admin.html` 和 `index.html`

### 检查浏览器控制台
1. 按 F12 打开开发者工具
2. 查看 Console 标签中的日志
3. 应该看到以下消息之一：
   - `✅ Using existing localStorage data (skip GitHub sync)`（正常）
   - `✅ Initial sync: Loaded data from GitHub`（首次加载）

### 手动清空 localStorage 重新开始
在浏览器控制台中运行：
```javascript
localStorage.removeItem('cvData');
location.reload();
```

## 🔄 数据流说明

### 正常流程（修复后）
1. **首次访问**：
   - localStorage 为空
   - 从 GitHub 载入初始数据（如果有）
   - 如果 GitHub 也没有，使用代码中的 defaultData

2. **后台修改**：
   - 保存到 localStorage
   - 尝试上传到 GitHub（异步，不阻塞）

3. **前台刷新**：
   - 检查 localStorage 是否有数据
   - 如果有，使用 localStorage 的数据（最新）
   - 如果没有，才从 GitHub 载入

### GitHub 同步说明
- 后台保存时会**尝试**上传到 GitHub
- 如果上传失败（没有 Token 或网络问题），数据仍然保存在 localStorage
- 前台优先使用 localStorage，确保能看到最新修改

## 📝 注意事项

1. **多设备同步**：如果在不同设备上编辑，需要手动清空 localStorage 才能看到 GitHub 上的最新数据
2. **Token 配置**：在后台的"GitHub Token 設定"区块配置 Token，才能上传到 GitHub
3. **浏览器隔离**：不同浏览器的 localStorage 是独立的，Chrome 的修改在 Edge 中看不到

## ✅ 验证修复成功

运行以下测试确认修复成功：

1. ✅ 后台添加工作经验 → 前台能立即看到（刷新后）
2. ✅ 后台修改个人资料 → 前台能立即看到
3. ✅ 后台添加技能 → 前台能立即看到
4. ✅ 清空 localStorage 后 → 前台显示默认数据或 GitHub 数据

## 🔧 调试命令

在浏览器控制台中使用这些命令进行调试：

```javascript
// 查看当前数据
console.log(JSON.parse(localStorage.getItem('cvData')));

// 查看工作经验
const data = JSON.parse(localStorage.getItem('cvData'));
console.table(data.experience.filter(e => e.type === 'Work'));

// 查看学历
console.table(data.experience.filter(e => e.type === 'Education'));

// 手动添加测试数据
const testWork = {
  id: crypto.randomUUID(),
  type: 'Work',
  company: '手动测试公司',
  role: '测试职位',
  period: '2024',
  summary: '通过控制台添加的测试数据'
};
data.experience.unshift(testWork);
localStorage.setItem('cvData', JSON.stringify(data));
location.reload(); // 刷新查看

// 清空所有数据
localStorage.clear();
location.reload();
```
